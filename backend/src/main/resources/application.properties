server.port=8080
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=6MB
# SQLite path; overridable via env SRA_DB_PATH
sra.db.path=${SRA_DB_PATH:data/smart_resume_analyzer.db}
