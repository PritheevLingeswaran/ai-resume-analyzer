// Deployment defaults for static hosting.
// Leave BASE_URL empty to use same-origin, or set it to a deployed API URL later.
(function(scope){
  scope.SRA_DEPLOY_CONFIG = {
    BASE_URL: "https://ai-resume-analyzer-bk7j.onrender.com",
    USE_MOCKS: false
  };
})(window);
